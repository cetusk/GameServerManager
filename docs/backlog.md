# 今後の課題・棚卸し

最終更新: 2026-09-19。基準リリース: v0.1.0。

ユーザーから挙がった課題を A〜L の固定 ID で管理します。すべて実装には未着手です。ユーザー方針として、細かな修正・改善は v0.1.x で段階的に公開し、D の複数ワールド管理は v0.2.0 を目標とします。A のログ履歴削除・L の Satisfactory 修正を直近の v0.1.1 の候補とし、調査・検証結果に応じて分割します。以下の限定する機能範囲、完了条件、着手順は検討案であり、対応済みの機能や全件の同時リリースを示すものではありません。

## 管理方法

- 状態は「未着手 → 調査中 → 方針決定 → 実装中 → 検証待ち → 完了」。保留時は理由と再開条件を記録します。
- 着手時に対象ゲーム・操作・採用する完了条件を確定します。課題を分割しても元の ID との対応を残します。
- 更新時は状態、未解決事項、関連する Issue／PR／コミット、確認結果を追記します。現時点では GitHub Issue は未作成です。
- 完了時は対応リリースと検証環境を記録します。人工データのテストと Windows 実ゲームでの確認は分けます。
- ゲーム固有の実装はゲーム・課題ごとの短期ブランチ＋worktree、共通機能は課題ごとの専用ブランチで進めます。検証後に main へ統合し、ゲーム別の長期ブランチは維持しません。

## バージョンの振り分け方針

- **v0.1.x**: 既存の管理単位・設定形式を保ち、不具合修正と範囲を絞った操作改善を行います。v0.1.1 を最初のまとまりとし、必要に応じて v0.1.2、v0.1.3 と段階的に公開します。
- **v0.2.0**: 複数ワールドなど管理対象を拡張する機能、保存方式や設定の移行を伴う変更を扱います。D を目標とし、関連する大きな機能は個別に範囲を決めます。
- 見た目の小ささや変更行数よりも、データ形式・対象範囲・起動停止・復元への影響で判断します。B の非表示化、H の自動削除、L の保存範囲検査は、短い修正でも検証範囲が広がる可能性があります。
- 小さな改善部分と構造変更部分を分離できる課題は、同じ ID の下で段階を分けます。分離できない場合は振り分けを再検討し、v0.1.x のために無理に変更範囲を広げません。
- 以下の「規模」は事前の見立てです。候補がすべて完了することを各バージョンの公開条件にはせず、調査後に採用範囲を確定します。

### 段階リリースの案

| 段階 | 候補 | 公開範囲の決め方 |
|---|---|---|
| v0.1.1 | A の履歴削除、L の設定保存修正、I の設定案内 | 直近の使い勝手・不具合を中心にする。L の調査や A の対象整理が長引く場合、完了した独立の修正を先に公開し、残りを後続へ送る |
| 後続の v0.1.x | F の接続情報、G の手動接続先登録、B の非表示化、H の保持上限、および持ち越し | F・G は関連する表示改善としてまとめる案。B と H は起動停止・データ削除に関わるため、各々の検証が完了した単位で分ける |
| v0.2.0 | D を軸に、J・K・E・C の採用範囲を決定 | 管理単位・移行・保存方式を設計してから、実装する機能を確定する |

後続項目を今の段階で v0.1.2／v0.1.3 に固定しません。依存関係、影響範囲、実機確認の完了状況で次の1リリース分を選びます。緊急の不具合修正は機能追加と分離して先に公開できます。各課題のリリース系列と、直近リリースへの採用判断は別に管理します。

## 一覧

| ID | 課題 | 目標／候補バージョンと範囲 | 規模の見立て | 状態 | 主な関連 |
|---|---|---|---|---|---|
| A | ログの履歴を消せるようにする | **v0.1.x／直近 v0.1.1 候補**。削除する履歴の種類を先に確定 | 小〜中 | 未着手 | H |
| B | ターミナルを表示しない | v0.1.x 候補。既存の起動停止方式を維持できる範囲 | 中 | 未着手 | C |
| C | 稼働中のホットスナップショット | v0.2.0 候補。対応ゲームを絞って段階導入 | 大 | 未着手 | D、E、H |
| D | 複数ワールドの管理 | **v0.2.0 目標**。切り替えと同時起動の範囲を先に確定 | 大 | 未着手 | C、E、H、J |
| E | キャラクターデータの管理 | v0.2.0 候補。対象ゲーム・操作を先に確定 | 大 | 未着手 | C、D、H |
| F | サーバー情報（IP:port）の表示 | v0.1.x 候補。既知の設定・ローカル情報の表示とコピー | 小〜中 | 未着手 | G |
| G | playit.gg／Tailscale の接続先情報 | v0.1.x 候補は手動登録・表示。自動取得は v0.2.0 以降の候補 | 小／中〜大 | 未着手 | F |
| H | バックアップ上限と古い世代の自動削除 | v0.1.x 候補。管理ツールのバックアップの保持件数に限定する案 | 中 | 未着手 | C、D、E |
| I | ARK SA の config.toml がない件 | v0.1.x／直近 v0.1.1 候補。現行 INI の案内・作成／登録導線。形式変更は J へ | 小（案内のみの場合） | 未着手 | J |
| J | 管理用 TOML・共通操作と公式設定形式の両立 | v0.2.0 候補。基本方針を記録、移行を伴う実装は別途決定 | 大 | 方針決定（詳細設計待ち） | D、I、K、L |
| K | Windrose／Conan のワールド項目を GUI で編集 | v0.2.0 候補。ゲーム・設定項目ごとに分割 | 中〜大 | 未着手 | J |
| L | Satisfactory の設定保存が SaveGames 検査で失敗 | **v0.1.x／直近 v0.1.1 候補**。原因を確認し、既存環境への修正を限定 | 未確定 | 未着手 | J |

## 課題ごとの確認事項・完了条件案

### A — ログ履歴の削除

- 要望: ログの履歴をユーザー操作で消せるようにする。
- 確認事項: 管理ツールの作業履歴、サーバーログ、SteamCMD ログのどこまでを対象にするか。画面上の履歴消去と実ファイルの削除・切り詰めをどう扱うか。稼働中の書き込みやログ監視との整合。
- 完了条件案: 消去対象を選べて結果が画面に反映される。登録情報・未完了操作の回復記録を巻き込まず、失敗時にもログを読み続けられる。

### B — ターミナルの非表示

- 要望: 操作時にターミナルを出さない。
- 確認事項: 管理 GUI 起動用のスクリプト、ゲームサーバー、SteamCMD、停止用ヘルパーのどれが表示されるかを Windows 上で特定する。
- 完了条件案: 通常操作で不要なウィンドウが出ず、ログ取得・エラー表示・サーバーの正常停止が引き続き動く。非表示化に伴うコンソール制御の変更も確認する。

### C — ホットスナップショット

- 要望: サーバー稼働中にもスナップショットを取得する。現状の管理ツールのバックアップは停止中のみ。
- 確認事項: ゲームごとの保存要求・完了確認・整合性のある取得方法。DB、分割ワールド、キャラクターデータを含む範囲と、稼働中取得に対応できないゲームの扱い。
- 完了条件案: 対応ゲームで復元・ロードを確認できるスナップショットを作成し、失敗・未完了のコピーを成功扱いしない。復元を稼働中に許可するかは別の判断事項とする。

### D — 複数ワールドの管理

- 要望: 同一ゲームの複数ワールドを管理する。現状は各ゲームにつきサーバー1件・管理対象ワールド1つ。
- 確認事項: 1台のサーバーでワールドを切り替える運用と、複数サーバーを登録する運用の対象範囲。同時起動の要否、ポート・設置先・設定・保存先の共有可否。
- 完了条件案: ワールドを識別して登録・選択でき、操作中の切り替えで対象が変わらない。バックアップ・復元・履歴が対象ごとに分かれ、既存登録を移行できる。

### E — キャラクターデータの管理

- 要望: キャラクターデータを管理対象にする。
- 確認事項: ゲームごとにサーバー側／クライアント側のどちらがデータを持つか。管理操作を一覧表示・バックアップ・復元・移行・編集のどこまでとするか。ワールドとの対応関係。
- 完了条件案: 対象ゲームと操作を明示し、別プレイヤーや別ワールドのデータと取り違えずに処理できる。ワールドと組み合わせた復元の確認方法を定める。

### F — サーバー情報（IP:port）の表示

- 要望: 接続に使う IP アドレスとポートを管理画面で確認できるようにする。
- 確認事項: LAN 内、外部公開、待受アドレスの区別。複数 NIC、IPv6、ゲーム接続用・検索用・管理用ポートの扱い。
- 完了条件案: 接続先の用途と取得元が分かり、コピーできる。外部から接続できるか未確認の情報や、取得できない情報を明示する。

### G — playit.gg／Tailscale の接続先情報

- 要望: サービスの操作統合よりも、フォワード先のドメインまたは IP を知りたい。ユーザー記載の「talescale」は Tailscale を指す想定。
- 確認事項: 利用者へ案内する公開接続先とローカル転送先のどちらを表示するか。手動登録か既存設定からの読み取りか。playit.gg の割当先と Tailscale の IP／DNS 名の違い。
- 完了条件案: F と合わせてドメイン／IP・必要なポート・用途を表示できる。変更・未取得時の扱いを決める。トンネル作成やサービスへのログイン連携は別途要望があれば検討する。

### H — バックアップの保持上限

- 要望: 上限を設け、古いバックアップから削除する。
- 確認事項: 件数・容量・保存期間のどれで上限を設定するか。ゲーム自身のバックアップと管理ツールのスナップショットの対象区分。手動保護、設定保存前・復元前の退避データの扱い。
- 完了条件案: 新しいバックアップが正常に完成した後、設定した単位・上限に従って古い世代を削除する。作成失敗時、未完了の回復に必要な世代、他ワールドの世代を誤削除しない。

### I — ARK SA の config.toml がない件

- 報告: ARK SA で `config.toml` が見当たらない。
- 現状: ARK SA は `Profile/*.ini` 相当のプロファイル INI を登録し、ほかの4ゲームは管理用 TOML を使う。新規作成でも ARK SA はプロファイル INI と `GameUserSettings.ini` を作成する。TOML がないことだけでは生成失敗と断定できない。
- 実装確認: 新規作成の保存ダイアログの初期名は `profile.ini`。現行版で `config.toml` がないこと自体は個人環境の不具合ではない。旧ツール由来の管理用プロファイル INI と、ARK 公式が読むゲーム用 INI は用途が異なる。
- 確認事項: 新規作成・既存登録のどちらで困ったか、必要なプロファイルが生成／選択できるか、GUI の案内が実際の形式と一致しているか。
- 完了条件案: ARK SA で必要なファイルと作成／登録手順が明確になる。管理用 TOML への統一と既存プロファイルの移行方式は J で扱う。
- 参照: [新規設定の作成](server-settings.md#新しいサーバーの設定を作成)、[操作ガイド](local-manager.md)。

### J — 管理用設定・操作の統一と公式形式の維持

- 要望: 公式サーバーアプリの仕様に準拠しつつ、管理アプリとしての使い方と管理用設定形式を統一する。
- 基本方針: 管理アプリ用は `config.toml` に統一し、ゲームが読む INI／JSON などの公式形式と必要な起動引数は維持する。GUI の作成・登録・編集の流れも共通化する。公式形式を TOML に置き換えてゲームへ渡す設計にはしない。
- ARK の構成案: 管理用 `config.toml` と、公式の `GameUserSettings.ini`／必要な `Game.ini` を役割別に持つ。現行の管理用 `profile.ini` は移行元として扱う案とし、管理用 INI と管理用 TOML を恒久的に二重管理する前提にはしない。
- 責務分担案: TOML は設置先・バックアップ先・SteamCMD・管理対象・起動設定などを保持する。ゲーム固有項目は公式設定から読み取り、GUI で変更した項目を対応する公式ファイルや起動引数へ反映する。同じ値を複数箇所に必要とする項目は、項目ごとに正とする保存先・反映方向を明示する。
- 詳細設計待ち: 共通 TOML のスキーマ、ゲーム固有設定との境界、外部編集との競合、初回作成と既存環境の取り込み、旧プロファイルの変換・退避・切り戻し、設定本文や秘密の重複保持の必要性。
- 完了条件案: 共通の管理用形式と操作で登録・編集でき、公式サーバーが必要な形式・引数で起動できる。未知のゲーム設定を保持し、外部編集や途中失敗を検出・回復できる。既存利用者の設定移行と切り戻しを検証する。基本方針の決定だけで実装完了にはしない。

### K — Windrose／Conan のワールド設定を GUI で編集

- 要望: 元の設定ファイルにあるワールド項目を GUI 上で編集したい。
- 現状: 基本的なサーバー設定の GUI はあるが、詳細なワールド項目は元ファイル編集の案内が中心。
- 確認事項: 必要な項目と実ファイルのキー、ゲームバージョンごとの配置・型・有効範囲。Windrose の WorldDescription や Conan の設定 INI への対応範囲。
- 完了条件案: 採用項目を GUI で読み書きし、元の設定ファイルへ反映できる。未知項目・秘密・ワールドの識別情報を壊さず、外部変更と保存失敗を扱える。ゲームで反映を確認する。

### L — Satisfactory の設定保存エラー

報告された操作記録:

```text
設定の保存 を開始
設定の保存 が失敗: SaveGames に専用サーバー以外のデータがあります。専用サーバーの保存領域を分離してから登録してください
```

- 確認できた実装: `crates/games/satisfactory/src/local.rs` の `validate_saves` は SaveGames 直下で `server`、`blueprints`、`ServerSettings*.sav` 以外の名前を検出すると、このエラーを返す。
- 未確認: 実際に拒否された項目名・種類、ゲームのビルド、初回設定か既存環境か。専用サーバーの正常な生成物を誤検出したのか、個人用データが混在しているのかは未確定。
- 次の調査: 設定保存の再現手順と直下の構成を確認する。個人用データを巻き込まない保存範囲を検討し、設定保存を止める必要がある条件と、利用者が解決できる案内を整理する。
- 完了条件案: 原因を再現して対応方針を決め、正しい専用サーバー構成では保存できる。不適切な構成では理由と対応方法が分かる。検査の一律解除や、実データの削除・移動を前提にしない。
- 参照: [Satisfactory の保存先に関する制約](server-settings.md)。

## 着手順の案

1. **I → A → F の順に検討**: まず現行形式の案内、履歴削除の対象、既知の接続情報表示を整理します。案内・表示だけで完結する部分から、小さく実装・検証します。
2. **L の原因調査は早期に実施**: 設定保存を妨げる報告なので後回しにせず、再現条件と拒否された項目を確認します。修正範囲が分かった時点で、直近の v0.1.x の実装順に組み込みます。
3. **B・G・H の限定範囲を検討**: B は正常停止、G は F との表示統合、H は復元・回復用バックアップの保持を確認します。影響が小さい部分を後続の v0.1.x の候補にします。
4. **次の v0.1.x の採用範囲を確定して検証・公開**: 最初は v0.1.1、その後は必要に応じて v0.1.2、v0.1.3 と進めます。大きな機能の設計完了や候補全件の完成を待たず、採用した修正と改善をまとめ、公開後に残りを棚卸しします。
5. **J・D を軸に v0.2.0 を設計**: 管理用 TOML と公式設定の責務分担・移行方法、および複数ワールドの管理単位を整理します。K はゲームごと、E は対象データごと、C は対応ゲームごとに分け、必要なものから採用します。D・E の管理単位と C・H の保存／保持対象を合わせます。

これは検討順の案で、実装の自動着手や優先度の確定を意味しません。実装前に変更範囲・互換性・必要な実機確認を整理し、調査結果に応じて順番を更新します。J の全面的な方式統一を、小さな修正の前提条件にはしません。

## 更新記録

| 日付 | 内容 |
|---|---|
| 2026-09-18 | A〜L の12件を登録。現行仕様、未確認事項、完了条件案、関連課題、着手順案を整理。実装には未着手。 |
| 2026-09-19 | ユーザー方針に沿い、小規模な修正・改善を v0.1.x で段階公開、D を v0.2.0 の目標として整理。A・L・I を直近 v0.1.1 の候補とし、後続のパッチ番号は検討結果で決める。他項目の候補バージョン・変更規模・限定範囲、影響の小さい変更からの検討順、ゲーム・課題ごとのブランチ運用も記録。 |
| 2026-09-19 | I の現行動作を確認。J は管理用 TOML・操作の統一と公式設定形式の維持を基本方針として記録。ARK の旧管理プロファイルと公式 INI を区別し、詳細設計・移行・実装は今後の課題とした。 |
